import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "localhost",
      "project-exterior-technical-test-app.up.railway.app",
      "i.travelapi.com",
    ],
  },
  /* config options here */
};

export default nextConfig;
